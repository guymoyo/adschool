// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package adschool.domain;

import adschool.domain.Evaluation;
import adschool.domain.Periode;
import java.lang.Boolean;
import javax.persistence.EntityManager;
import javax.persistence.TypedQuery;

privileged aspect Evaluation_Roo_Finder {
    
    public static TypedQuery<Evaluation> Evaluation.findEvaluationsByActif(Boolean actif) {
        if (actif == null) throw new IllegalArgumentException("The actif argument is required");
        EntityManager em = Evaluation.entityManager();
        TypedQuery<Evaluation> q = em.createQuery("SELECT o FROM Evaluation AS o WHERE o.actif = :actif", Evaluation.class);
        q.setParameter("actif", actif);
        return q;
    }
    
    public static TypedQuery<Evaluation> Evaluation.findEvaluationsByPeriode(Periode periode) {
        if (periode == null) throw new IllegalArgumentException("The periode argument is required");
        EntityManager em = Evaluation.entityManager();
        TypedQuery<Evaluation> q = em.createQuery("SELECT o FROM Evaluation AS o WHERE o.periode = :periode", Evaluation.class);
        q.setParameter("periode", periode);
        return q;
    }
    
}
